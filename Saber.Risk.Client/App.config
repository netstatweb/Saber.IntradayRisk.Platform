<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <!--
    Connection string moved to server-side configuration:
    Saber.Risk.Api\appsettings.json -> ConnectionStrings:SaberRiskConnection

    PL: Connection string przeniesiony do pliku appsettings.json projektu API.
    Dzięki temu klient (WPF) nie trzyma poświadczeń/connection stringów i komunikuje się z serwerem przez API/SignalR.
  -->
</configuration>